# Default log level for all loggers
org.slf4j.simpleLogger.defaultLogLevel=info

# AzContentUnderstanding level is controlled per-plugin:
#   Surefire (unit tests)      -> OFF  (suppresses expected error-handling logs)
#   Failsafe (integration)     -> INFO (full visibility)
# See <systemPropertyVariables> in pom.xml
